# excelrouter skill 运行依赖（不含 customtkinter —— 这是命令行/skill 形态，没有桌面 GUI）
openpyxl>=3.1.0
pandas>=2.0.0
xlrd>=2.0.1

# 以下三个仅 PDF 加密分发（er_pdf_dist.py）需要；不装也不影响 Excel 拆分功能
pypdf>=6.0.0
fpdf2>=2.8.0
cryptography>=42.0.0
